# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Requirements for scripts/merge_lora.py (LoRA adapter merge).
#
# Installed separately from the backend so the FastAPI process does not
# take on the weight-heavy ML dependencies. The backend looks for the
# interpreter in this order (services/student_model_service.py):
#   1. the MERGE_LORA_PYTHON setting
#   2. {WORKSPACE_ROOT}/merge-lora-venv/bin/python (legacy per-workspace path)
#   3. ~/.local/share/vlm-feedback-loop/merge-lora-venv/bin/python
#      (shared Profile C path provisioned by scripts/setup-dev.sh)
#   4. its own interpreter (works only if you install these deps there)
#
# Provision the shared dedicated venv:
#   uv venv "$HOME/.local/share/vlm-feedback-loop/merge-lora-venv"
#   uv pip install --python \
#     "$HOME/.local/share/vlm-feedback-loop/merge-lora-venv/bin/python" \
#     -r scripts/merge_lora_requirements.txt

transformers>=4.41
peft>=0.11
torch>=2.3
accelerate>=0.30
safetensors>=0.4
